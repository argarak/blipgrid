@require "defines.styl"
@require "input-common.styl"

:host
  position relative

#trackTabs
  display flex
  flex-direction row
  width 100%
  margin-bottom 10px
  justify-content space-around

  .active
    background var(--surface-active)
    color var(--primary-text)
    border-color var(--primary-color)

  .solo
    color var(--solo-color)

  .mute
    color var(--mute-color)

#viewTitle
  top 100px
  left 0

  display flex
  flex-direction row
  justify-content start
  align-items center
  position absolute

  color var(--note-color)
  background var(--surface-active)

  border-right 5px solid var(--on-surface-active)

  padding 10px

  span
    margin-right 10px

.trackTab
  background var(--surface-background)
  height 100%
  padding 5px 25px
  width 100%
  display flex
  align-items center
  justify-content center
  border-bottom 1px var(--surface-active) solid

  transition all .1s ease-in-out
  cursor pointer
  user-select none

#stepsContainer
  display flex
  flex-direction row
  justify-content center
  align-items center

#sequenceGridContainer
  flex 1
  display flex
  align-items start
  overflow-y hidden
  margin 10px 0px

#algorithmSelect
  width 100%
  max-width 40ch
  margin-top 5px
  margin-right 5px

#knobContainer
  display flex
  flex-direction row
  justify-content center
  align-items start

  width 100%
  min-height 120px

  ui-knob
    width 100%
    max-width 15ch
    margin 0px 15px

#progress
  width 100%
  height 1px
  background-color var(--surface-background-alt)

  div
    transition width .1s linear
    background-color var(--primary-color)
    height 1px
